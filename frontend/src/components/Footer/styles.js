import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 50px;
  color: var(--black);
  margin: auto;
  display: flex;
  align-items: center;
  justify-content: center;
`;
